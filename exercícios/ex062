resp 's'
soma quant media maior menor = 0
while resp in 'Ss':
num = int(input('Digite um número: '))
soma += num
quant+= 1
1: if quant
maior menor = num
else:
if num > maior:
maior num
if num < menor:
menor = num
resp = str(input('Quer continuar? [s/n]')).lower().strip()[0]
media soma / quant
print('Você digitou (quant) números e a media foi(media}')
